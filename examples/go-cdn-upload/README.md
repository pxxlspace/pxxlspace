# Go CDN Upload Example

Upload a file to Pxxl CDN with the official Go SDK.

```bash
PXXL_API_KEY=pxxl_... go run . ./logo.png
```

Create the API key in Pxxl with `scope=cdn` and `permission=read_write`.
