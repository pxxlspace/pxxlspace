# Node CDN Upload Example

Upload a file to Pxxl CDN with the official Node SDK.

```bash
npm install
PXXL_API_KEY=pxxl_... npm run upload -- ./logo.png
```

Create the API key in Pxxl with `scope=cdn` and `permission=read_write`.
